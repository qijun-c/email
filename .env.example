# ===== Backend/API =====
APP_ENV=development
# Postgres in docker-compose
DATABASE_URL=postgresql+psycopg://mail_user:mail_pass@db:5432/maildb
ATTACHMENTS_DIR=/data/attachments

# Mailgun settings (required for inbound + send)
MAILGUN_DOMAIN=
MAILGUN_API_KEY=
MAILGUN_SIGNING_KEY=

# Public site domain (used for generating full addresses & links)
PUBLIC_DOMAIN=qjlearn.qzz.io
PUBLIC_BASE_URL=http://localhost

# CORS origins (comma separated); use * for dev
CORS_ORIGINS=*

# ===== Frontend =====
# Base path for API calls from the frontend. Default to /api when behind proxy.
NEXT_PUBLIC_API_BASE=/api

# ===== Proxy (Caddy) =====
# If you want automatic HTTPS with your domain, set SITE_DOMAIN and EMAIL, and update Caddyfile accordingly.
SITE_DOMAIN=
ACME_EMAIL=
